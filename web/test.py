from Algoritma import *
import json

def jsonToDict(stuff):
    result = []
    for i in range(len(stuff)):
        temp
        result.append(json.loads(stuff[i]))
    
    return result


def findNearest(origin, listNode):
    distance = -1
    node = None
    for node in listNode:
        if node != origin:
            
    